<h1>Editer un comment</h1>


<%= form_for [@gossip, @comment] do |f| %>
  <div class=" form-group">
     <label>Contenu du Comment</label>
     <%= f.text_area :content, value: @comment.content, class: 'form-control'%>
  </div>  
  <div class="form-group">
  <button type="submit" class= "btn btn-primary"> Modifier le comment</button>
  </div>

 <% end %> 